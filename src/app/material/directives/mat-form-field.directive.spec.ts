import { MatFormFieldDirective } from './mat-form-field.directive';

describe('MatFormFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new MatFormFieldDirective();
    expect(directive).toBeTruthy();
  });
});
